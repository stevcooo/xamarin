<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"             
             x:Class="ClassesScheduler.Pages.StudyFieldsPage">
  <ContentPage.Content>
    <Grid>
      <Grid.RowDefinitions>
        <RowDefinition Height="Auto"/>
        <RowDefinition Height="*"/>
      </Grid.RowDefinitions>
      <Label Grid.Row="0" Text="Please select your field of study" FontSize="18"/>
      <ListView Grid.Row="1" ItemsSource="{Binding Items}" SelectedItem="{Binding SelectedItem}" SeparatorVisibility="None" >
        <ListView.ItemTemplate VerticalOptions="FillAndexpand">
          <DataTemplate>
            <ViewCell>
              <ViewCell.View>
                <Grid Padding="10,0,0,0">
                  <Label Text="{Binding Description}" HorizontalTextAlignment="Start" VerticalTextAlignment="Center"/>
                </Grid>
              </ViewCell.View>
            </ViewCell>
          </DataTemplate>
        </ListView.ItemTemplate>
      </ListView>
    </Grid>
  </ContentPage.Content>
</ContentPage>